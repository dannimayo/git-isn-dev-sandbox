<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PreventStageChangeClosed</fullName>
    <active>true</active>
    <description>Prevents changing stage if oppty is in Closed/Won or Lost/Dead</description>
    <errorConditionFormula>AND(
	 NOT($Profile.Name = &#39;System Administrator&#39;),
	 IsClosed,
	 ISCHANGED ( StageName ),
	 (RecordType.Name = &#39;Contractor Subscription&#39;)
 )</errorConditionFormula>
    <errorDisplayField>StageName</errorDisplayField>
    <errorMessage>Opportunity stage cannot be changed if stage is closed/won or lost/dead.</errorMessage>
</ValidationRule>
